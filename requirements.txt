# Mimir MVP — deliberately lean. No statsmodels: the model is OLS and the
# algebra is in app/model.py where it can be read.
fastapi>=0.115
uvicorn[standard]>=0.32
pandas>=2.2
numpy>=2.0
openpyxl>=3.1
